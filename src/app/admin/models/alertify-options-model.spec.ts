import { AlertifyOptionsModel } from './alertify-options-model';

describe('AlertifyOptionsModel', () => {
  it('should create an instance', () => {
    expect(new AlertifyOptionsModel()).toBeTruthy();
  });
});
